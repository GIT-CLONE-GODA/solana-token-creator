// Example configuration file for real token creation
// Copy this to config.js and update with your actual values
// NEVER commit config.js to version control!

const CONFIG = {
    // GitHub Configuration
    GITHUB_OWNER: 'your-github-username',
    GITHUB_REPO: 'solana-token-creator',
    PERSONAL_ACCESS_TOKEN: 'ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx', // Your GitHub Personal Access Token
    
    // Network Configuration
    DEFAULT_NETWORK: 'devnet', // 'devnet' or 'mainnet'
    
    // UI Configuration
    APP_NAME: 'Solana Token Creator',
    APP_VERSION: '1.0.0'
};

// Export for use in your application
if (typeof module !== 'undefined' && module.exports) {
    module.exports = CONFIG;
} else {
    window.CONFIG = CONFIG;
}